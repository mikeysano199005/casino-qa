[build]
builder = "nixpacks"

[deploy]
# "always" so the bot comes back after an intentional restart from the admin
# panel (which exits the process). "on_failure" would leave it down on a clean exit.
restartPolicyType = "always"
restartPolicyMaxRetries = 10
